[project]
name = "divbase_monorepo"
version = "0.1.0"
description = "Monorepo managed by uv"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "src/lib", 
    "src/api", 
    "src/cli"
]
